#include "Shape.h"
/*
FreeWill::Shape<1> createShape(unsigned int size)
{
    FreeWill::Shape<1> shape;
    shape[0] = size;
    return shape;
}

FreeWill::Shape<2> createShape(unsigned int batchSize, unsigned int size)
{
    return FreeWill::Shape<2>({size, batchSize});
}

FreeWill::Shape<3> createShape(unsigned int batchSize, unsigned int height, unsigned int width)
{
    return FreeWill::Shape<3>({width, height, batchSize});
}
*/
